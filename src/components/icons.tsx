import { Code2 } from "lucide-react";
import type { IconType } from "react-icons";

export const GenericTechIcon: IconType = ({ size, className, style }) => (
  <Code2 size={size} className={className} style={style} />
);
